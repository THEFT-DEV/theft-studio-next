# AGENTS.md

> Auto-generated by [agentsmith](https://github.com/jpoindexter/agentsmith)

## TL;DR

- **Stack**: Next.js 15.1.6 + TypeScript 5 + Tailwind 3.4.17
- **Components**: 3 total — USE EXISTING, don't create new
- **Key imports**: `cn()` from `@/lib/utils`
- **High-impact files**: content, utils (changes affect many files)
- **API**: 1 routes (0 protected)

**Rules**: Use design tokens (not hardcoded colors), use `cn()` for classes, check existing components first

## Getting Started

```bash
npm install

# Start development
npm run dev
```

## Project Overview

| | |
|---|---|
| **Framework** | Next.js 15.1.6 (App Router) |
| **Language** | TypeScript |
| **Styling** | Tailwind CSS |
| **Class Utility** | `cn()` from `@/lib/utils` |
| **Components** | 3 |
| **API Routes** | 1 |
| **Codebase** | 23 files, 1,604 lines |

## Critical Rules

**These rules are NON-NEGOTIABLE:**

### 1. USE EXISTING COMPONENTS
This project has 3 components. Check the list below before creating anything new.

```tsx
// WRONG
<div className="rounded border p-4">...</div>

// RIGHT
<Card><CardContent>...</CardContent></Card>
```

### 2. USE DESIGN TOKENS
Never hardcode colors. Use semantic tokens that work with theme switching.

```tsx
// WRONG
className="bg-blue-500 text-white"
style={{ color: "#3b82f6" }}

// RIGHT
className="bg-primary text-primary-foreground"
```

### 3. USE `cn()` FOR CLASSES
Import from `@/lib/utils`. Handles conditional classes correctly.

```tsx
// WRONG
className={"btn " + (active ? "btn-active" : "")}

// RIGHT
className={cn("btn", active && "btn-active")}
```

### Framework-Specific

- Use `'use client'` directive only when component needs interactivity
- Prefer Server Components by default
- Use `next/image` for images, `next/link` for navigation
- Never use arbitrary values (`w-[137px]`) — use scale values
- Type all props and function parameters
- Avoid `any` — use proper types or `unknown`

## Components

3 components across 1 categories.

### Layout (3)

- `Footer` — `@/components/layout/Footer`
- `Header` — `@/components/layout/Header`
- `Navigation` — `@/components/layout/Navigation`

## Utilities

- `cn(...classes)` — Class merging utility from `@/lib/utils`
- `formatDate()` — from `@/lib/utils`
- `stripHtml()` — from `@/lib/utils`

## Most Imported Files

These files are imported most frequently - changes here have wide impact:

- `src/lib/content.ts` — imported by 6 files
- `src/lib/utils.ts` — imported by 4 files

## ⚠️ Potentially Unused Components

These component files are never imported anywhere:

- `src/components/layout/Navigation.tsx`

*Consider removing these or they may be entry points not detected.*

## Key Dependencies

Most used external packages:

- `next` — 18 imports
- `fs` — 1 imports
- `path` — 1 imports
- `clsx` — 1 imports
- `react` — 1 imports

## API Routes

1 API endpoints:

### Contact

- `POST` `/api/contact`

## Design Tokens

### Color Tokens

**Use semantic classes, never hardcoded colors:**

```
// Backgrounds
bg-background, bg-card, bg-muted, bg-primary, bg-secondary, bg-destructive, bg-accent

// Text
text-foreground, text-muted-foreground, text-primary-foreground, text-destructive

// Borders
border-border, border-primary, border-destructive

// NEVER USE:
// bg-white, bg-black, bg-gray-500, text-gray-600, #ffffff, rgb(...)
```

<details>
<summary>Raw CSS Variables (click to expand)</summary>

```css
--background: #ffffff;
--foreground: #171717;
```

</details>

## Additional Guidelines

**Using <a> instead of Next.js Link**
- Wrong: `<a href="/about">About</a>`
- Right: `<Link href="/about">About</Link>`

**Using <img> instead of Next.js Image**
- Wrong: `<img src="/photo.jpg" alt="Photo" />`
- Right: `<Image src="/photo.jpg" alt="Photo" width={400} height={300} />`

**Missing 'use client' directive**
- Wrong: `// Component with useState but no directive`
- Right: `'use client'`

**Unnecessary 'use client'**
- Wrong: `'use client'`
- Right: `export function StaticCard({ title }) { return <div>{title}</div> }`

**Unvalidated API inputs**
- Wrong: `const { id } = req.body; // No validation`
- Right: `const { id } = schema.parse(req.body);`

## AI Assistant Configuration

**Recommended Settings Based on Codebase Complexity:**

- **For simple tasks** (formatting, typing): minimal effort (fast, low-cost models)
- **For complex tasks** (refactoring, architecture): standard effort (balanced cost/capability)

**Complexity by Area:**

- 🟢 **API Routes** (1 files, complexity: 20/100)
  - Recommended: Minimal effort (fast, low-cost model)
  - Characteristics: complex regex patterns
- 🟢 **Pages/Routes** (8 files, complexity: 10/100)
  - Recommended: Minimal effort (fast, low-cost model)
  - Characteristics: complex regex patterns
- 🟢 **Source Files** (2 files, complexity: 5/100)
  - Recommended: Minimal effort (fast, low-cost model)
  - Characteristics: complex regex patterns
- 🟢 **Utilities** (2 files, complexity: 5/100)
  - Recommended: Minimal effort (fast, low-cost model)
  - Characteristics: complex regex patterns
- 🟢 **Configuration** (2 files, complexity: 3/100)
  - Recommended: Minimal effort (fast, low-cost model)

**High Complexity Files** (use most capable model with extended thinking):

- `src/app/api/contact/route.ts` (56 lines, score: 20/100)
  - complex regex patterns
- `src/app/page.tsx` (179 lines, score: 16/100)
  - complex regex patterns
- `next-env.d.ts` (7 lines, score: 10/100)
  - complex regex patterns

## Commands

```bash
npm run dev          # next dev
npm run build        # next build
npm run lint         # next lint
```

### Other Commands

```bash
npm run start
```
